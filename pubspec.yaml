name: vora_climate
description: Vora Phase 2 Skill Task — Room Climate Control Interface
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Provider is Flutter's recommended, lightweight state management.
  # It wraps InheritedWidget (Flutter's native DI) in a simpler API.
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
